# Models

Place model weights here:

- `best.pt` — YOLOv11 detector
- `swin_classifier.pth` — Swin Transformer classifier (38 PlantVillage classes)

These files are ignored by git due to size. Download or copy manually.
